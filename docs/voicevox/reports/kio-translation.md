# 成田基生日语翻译初稿记录

2026-09-17：完成当前有效剧本中“成田基生”与“基生”两个原始标签的全部日语初稿。两者按同一配音角色“成田基生”合并，同时保留原始说话人标签供追溯。

- 台词总数：446 条。
- 原始标签：“成田基生” 27 条，“基生” 419 条。
- 本次新增：400 条；保留既有序章初稿 46 条。
- 覆盖范围：序章、第一章、第二章、第三章、女主视角、最终章。
- 顺序：按全剧 `order` 从 2 到 1593 递增。
- 状态：全部为 `draft`，需人工校对口吻、专名读音、断句和 VOICEVOX 听感；未标记为 `proofread`。

主导出文件为 `game/data/voice/translations/export/speakers/成田基生.tsv`；VOICEVOX 批次导入文件为 `game/data/voice/voicevox-import/成田基生.tsv`。两个文件均含 446 条，不再生成独立的“基生.tsv”。
