# I.L.Y. 项目手动微调指南

本文面向之后要手动修改剧情、图片和音乐的人。先记住项目最核心的三层：

1. **素材文件**：真正的 PNG、JPG、WebP、MP3 等文件。
2. **素材清单**：给文件起一个稳定的“资源 ID”，剧情只引用这个 ID。
3. **剧情节点**：决定这一句文字出现时使用哪张图、哪首音乐，以及下一句去哪里。

标准流程是：**把素材放进正确文件夹 → 在素材清单登记 ID → 在剧情节点引用 ID → 启动游戏检查**。

> 重要：路径一律相对 `game/index.html`，使用 `/`，不要填写 `E:\...` 这样的电脑绝对路径。

---

## 一、图片怎么插入

### 1. 图片应该放在哪里

| 图片用途 | 建议目录 |
| --- | --- |
| 场景背景 | `game/assets/images/backgrounds/` |
| 人物透明立绘 | `game/assets/images/characters/` |
| 整屏 CG | `game/assets/images/cg/` |
| 照片、快递单等道具/UI | `game/assets/images/ui/` |
| RPG 地图和行走素材 | `game/assets/images/maps/`、`game/assets/images/objects/` |

背景建议使用 16:9 图片；人物立绘建议使用透明背景的 PNG 或 WebP。

### 2. 在素材清单中登记图片

序章和第一章的主要清单在：

```text
game/data/assets.js
```

第二章以后补充的图片清单在：

```text
game/data/chapter-assets.js
game/data/map-assets.js
```

例如新放入一张图片：

```text
game/assets/images/backgrounds/rainy-street.webp
```

在 `game/data/assets.js` 的 `images` 中登记：

```js
"bg-rainy-street": "assets/images/backgrounds/rainy-street.webp",
```

这里：

- `bg-rainy-street` 是资源 ID，可以自行命名，但不能和现有 ID 重复。
- 右侧是实际文件路径，必须和磁盘上的文件名、扩展名完全一致。
- 剧情文件中写资源 ID，不直接重复写文件路径。

如果只是替换已有图片，最简单的方式是保持原文件名不变，直接替换文件；或者只修改资源清单右侧的路径，不修改左侧 ID。这样所有引用该 ID 的剧情都会一起更新。

> `game/data/chapter-assets.js` 是 `tools/chapters/build.py` 生成的。直接改它可以立即生效，但以后重新生成第二、三、最终章时会被覆盖。最省事的长期做法是沿用它规定的文件名；需要改路径规则时，应同步修改 `tools/chapters/build.py`。

### 3. 在剧情节点中使用图片

标准对白节点大致如下：

```js
{
  type: "dialogue",
  speaker: "成田基生",
  text: "雨还没有停。",
  background: "bg-rainy-street",
  portrait: "portrait-kio",
  next: "下一节点ID"
}
```

可用的主要图片字段：

| 字段 | 用途 | 说明 |
| --- | --- | --- |
| `background` | 普通整屏背景 | 默认铺满屏幕，可能裁掉边缘 |
| `portrait` | 单个人物立绘 | 默认显示在画面中央 |
| `characters` | 单人或多人立绘 | 可以控制左、中、右和缩放；存在时优先于 `portrait` |
| `overlay` | 照片、信件、道具 | 居中悬浮显示 |
| `cg` | 整屏 CG | 会替代普通背景，并隐藏人物和 `overlay` |
| `backgroundFit` | 图片适配方式 | `cover` 铺满并可能裁切；`contain` 完整显示并可能留边 |
| `backgroundPosition` | 裁切焦点 | 例如 `"60% center"`，可把重要内容向画面中间保留 |

#### 单人立绘

```js
portrait: "portrait-airi"
```

#### 多人、站位和缩放

```js
characters: [
  { image: "portrait-kio", position: "left", scale: 0.95 },
  { image: "portrait-airi", position: "right", scale: 1.05, speaking: false }
]
```

- `position` 可写 `left`、`center`、`right`。
- `scale` 当前会被限制在 `0.5` 到 `1.4` 之间。
- `speaking: false` 会把该人物稍微压暗。

#### 道具或照片

```js
overlay: "parcel-label"
```

#### 整屏 CG

```js
cg: "ch2-hug",
backgroundFit: "contain",
backgroundPosition: "center"
```

`cg` 和人物立绘、道具图不要在同一个节点中混用，因为当前渲染规则会在存在 `cg` 时忽略人物和 `overlay`。

### 4. 怎样让图片只在某一段剧情中显示

这是最容易混淆的一点：**图片没有“从 A 节点持续到 B 节点”的自动范围设置。每进入一个剧情节点，旧画面都会被清空并重新创建。**

因此，要让同一张图覆盖连续三句，就在这三个节点中都写同一个图片 ID；范围后的第一个节点改成下一张图，或者不再写人物字段。

```js
scene_100: {
  type: "dialogue",
  speaker: "旁白",
  text: "她站在雨中的街口。",
  background: "bg-rainy-street",
  portrait: "portrait-airi",
  next: "scene_101"
},

scene_101: {
  type: "dialogue",
  speaker: "爱理",
  text: "你终于来了。",
  background: "bg-rainy-street",
  portrait: "portrait-airi",
  next: "scene_102"
},

scene_102: {
  type: "dialogue",
  speaker: "基生",
  text: "抱歉，让你久等了。",
  background: "bg-rainy-street",
  portrait: "portrait-airi",
  next: "scene_103"
},

scene_103: {
  type: "dialogue",
  speaker: "旁白",
  text: "两人回到了公寓。",
  background: "bg-apartment-dusk",
  next: "scene_104"
}
```

上例中：

- `bg-rainy-street` 和爱理立绘显示在 `scene_100` 至 `scene_102`。
- 到 `scene_103` 时背景换成公寓。
- `scene_103` 没有 `portrait`，所以爱理立绘也在这里消失。

不要把节点中的 `scene: "S01"` 当成显示范围。`scene` 主要是章节分组和存档信息，真正控制画面的是每个节点上的 `background`、`portrait`、`characters`、`overlay`、`cg`。

如果同一场景几十个节点都要同一背景，第一章以后的生成脚本已经会把场景背景复制到每个生成节点中：

- 第一章场景背景表：`tools/chapter1/build.py` 中的 `backgrounds`。
- 第二、三、最终章场景背景表：`tools/chapters/build.py` 中的 `background()`。
- 某段 CG 的开始和结束：`tools/chapters/build.py` 的 `special()` 中通过 `self.cg = "资源ID"` 开始，通过 `self.cg = None` 结束。

这比手工给几十个生成节点逐一粘贴字段更适合长期维护。

### 5. 图片大小和位置在哪里调整

单个背景可以直接在剧情节点中微调：

```js
backgroundFit: "cover",
backgroundPosition: "65% center"
```

单个人物需要站位或缩放时，用 `characters` 代替 `portrait`。

如果要改变**全项目所有**背景、立绘或道具的默认大小和位置，修改：

```text
game/styles/stage.css
```

主要样式是：

```css
.scene-background  /* 背景 */
.portrait          /* 人物 */
.scene-prop        /* 道具/照片 */
```

这种修改会影响所有剧情画面，调整前应在桌面和手机尺寸下都检查一次。

### 6. RPG 情节中的图片不完全一样

`type: "rpg"` 的节点主要根据地图数据绘图，而不是根据普通对白的 `background` 绘图。

相关位置：

- 第一章地图真源：`game/data/maps/chapter1.json`
- 第二、三章地图：`game/data/maps/chapters.json`
- 地图背景资源：地图对象中的 `art.background`
- 地图物件图：事件或物件中的 `image`
- 调查后弹出的预览图：事件中的 `preview`
- 对应资源 ID：`game/data/map-assets.js` 或其他图片清单

修改 JSON 后运行：

```powershell
npm run build:maps
```

该命令会同步生成供 `file://` 预览使用的 `chapter1-bundle.js` 和 `chapters-bundle.js`。不要直接修改这两个 `*-bundle.js` 文件。

---

## 二、音乐怎么插入，以及怎样控制播放范围

### 1. 当前音乐系统的实际状态

目前项目已经有 BGM 播放能力，但：

- `game/assets/audio/bgm/` 中目前没有正式音乐，只有用于保留文件夹的 `.gitkeep`。
- `game/data/assets.js` 已预先登记 `bgm-dusk`、`bgm-university`、`bgm-coast` 等 ID，但对应 MP3 目前不存在。
- 当前剧情节点中还没有实际的 `bgm` 字段，所以现在游戏不会自动切入 BGM。
- `sfx` 和 `voices` 目前只是登记了路径，尚未接入播放逻辑。也就是说，当前可以按下面方法加入的是**循环背景音乐 BGM**；音效和配音仅放文件、填清单还不会自动播放。

### 2. 添加一首 BGM

把音乐放入：

```text
game/assets/audio/bgm/
```

例如：

```text
game/assets/audio/bgm/rain-theme.mp3
```

然后在 `game/data/assets.js` 的 `bgm` 中登记：

```js
"bgm-rain": "assets/audio/bgm/rain-theme.mp3",
```

### 3. 控制音乐的开始、延续、切换和停止

音乐和图片的范围规则不同。BGM 是持续状态：

- 在一个节点写 `bgm: "某个ID"`：从这个节点开始播放或切换。
- 后续节点不写 `bgm`：继续上一首，不会因换句而重播。
- 写 `bgm: null`：从这个节点停止。
- 写另一个 BGM ID：从这个节点切换成新音乐。

```js
scene_200: {
  type: "dialogue",
  speaker: "旁白",
  text: "雨声渐渐靠近。",
  background: "bg-rainy-street",
  bgm: "bgm-rain",
  next: "scene_201"
},

scene_201: {
  type: "dialogue",
  speaker: "爱理",
  text: "听见了吗？",
  background: "bg-rainy-street",
  next: "scene_202"
},

scene_202: {
  type: "dialogue",
  speaker: "基生",
  text: "嗯。",
  background: "bg-rainy-street",
  next: "scene_203"
},

scene_203: {
  type: "dialogue",
  speaker: "旁白",
  text: "雨停了，四周重新安静下来。",
  background: "bg-apartment-night",
  bgm: null,
  next: "scene_204"
}
```

这首音乐的范围就是 `scene_200` 开始，到进入 `scene_203` 时停止。

### 4. 当前 BGM 的限制

目前只有普通 `dialogue` 和 `choice` 节点会处理 `bgm` 字段。`phone`、`rpg`、`walk`、`battle`、`boss` 等节点即使写了 `bgm`，当前代码也不会在进入该节点时执行音乐切换。

因此现在应这样安排：

- 要让音乐覆盖一个 RPG/手机/战斗段，在进入它之前的对白节点启动 BGM。
- 要在该段结束后停止或换曲，在返回普通对白后的第一个节点写 `bgm: null` 或新的 BGM ID。
- 如果必须在非对白节点进入的一瞬间切歌，需要以后把音乐处理从 `game/src/modes/dialogue.js` 移到 `game/src/main.js` 的统一节点入口；这属于引擎功能修改，不是单纯改数据。

播放器的其他现状：

- BGM 默认循环播放。
- 默认音量是 `0.4`，在 `game/src/core/assets.js` 中设置。
- 玩家必须在右上角菜单点击“开启音乐”；浏览器不允许网页在没有用户操作时擅自播放声音。
- 相同 BGM ID 连续触发时不会从头重播。
- 当前没有淡入、淡出、单曲只播一次等配置。

---

## 三、剧本表述应该在哪里修正

### 1. 文件对应表

| 要修改的内容 | 长期维护时应改的文件 | 游戏实际读取/生成的文件 |
| --- | --- | --- |
| 序章普通对白、旁白、选项 | `game/data/story/prologue.js` | 同一个文件，直接生效 |
| 序章手机邮件、联系人等 | `game/data/story/phone.js` | 同一个文件，直接生效 |
| 第一章正文 | `tools/chapter1/source.txt` | 生成 `game/data/story/chapter1.js` |
| 第二章正文 | `tools/chapters/第二章游戏剧情脚本.txt` | 生成 `game/data/story/chapter2.js` |
| 第三章正文 | `tools/chapters/第三章游戏剧情脚本.txt` | 生成 `game/data/story/chapter3.js` |
| 最终章正文 | `tools/chapters/最终章游戏剧情脚本.txt` | 生成 `game/data/story/final.js` |
| 第一章 RPG 地图名称、调查提示 | `game/data/maps/chapter1.json` | 同步生成 `chapter1-bundle.js` |
| 第二、三章 RPG 地图名称、调查提示 | `tools/chapters/maps.py`（长期）或 `game/data/maps/chapters.json`（临时） | 同步生成 `chapters-bundle.js` |
| 菜单、系统通知、存档等界面用语 | `game/src/i18n.js` | 同一个文件 |
| 某个玩法内部写死的提示 | `game/src/modes/` 下对应模式文件 | 同一个文件 |

### 2. 最推荐的查找方法

如果只知道要改的原句，不知道属于哪一章：

1. 在编辑器中按 `Ctrl+Shift+F` 打开全项目搜索。
2. 粘贴原句中一段有辨识度的文字。
3. 先看它是否出现在 `tools/.../*.txt` 源稿中，再看是否出现在 `game/data/story/*.js` 中。
4. 有源稿和生成文件两份时，优先改源稿，然后重新生成。

不要只搜索说话人姓名，因为同一个名字可能出现几百次；搜索完整短句通常最快。

### 3. 直接改哪些字段

在序章或临时修改生成文件时，通常只改：

```js
speaker: "说话人",
text: "实际显示的句子"
```

选项按钮文字在：

```js
choices: [
  { text: "选项文字", next: "目标节点ID" }
]
```

手机邮件正文在 `game/data/story/phone.js` 的：

```js
body: "邮件正文"
```

只修表述时，尽量不要动这些结构字段：

- 节点 ID，例如 `ch2_123`、`s02a`。
- `next` 和 `choices[].next`。
- `flag`、`route`、`when`。
- `type`、`map`、`task`、`level`。

这些字段连接剧情分支和玩法；改错会造成跳错场景、断线或旧存档失效。

### 4. 生成文件为什么不能只改成品

以下文件开头明确写着 `Generated by ...`：

```text
game/data/story/chapter1.js
game/data/story/chapter2.js
game/data/story/chapter3.js
game/data/story/final.js
game/data/maps/chapter1-bundle.js
game/data/maps/chapters-bundle.js
```

直接修改它们会马上生效，适合快速试一句文案或临时定位问题；但下次运行生成脚本后，修改会丢失。因此正式修改应该回到源 TXT、JSON 或生成脚本。

第一章正文修改后运行：

```powershell
python tools/chapter1/build.py
```

第二、三、最终章任一正文修改后运行：

```powershell
python tools/chapters/build.py
```

这个命令会一起重新生成第二章、第三章、最终章和 `game/data/chapter-assets.js`。如果电脑上 `python` 命令不可用，可以尝试把命令开头换成 `py`。

地图 JSON 修改后运行：

```powershell
npm run build:maps
```

### 5. 修改生成源稿时的特殊警告

第一章以后有一些图片切换、CG 起止、分支和玩法入口，是生成脚本通过“识别某句原文”插进去的。例如脚本中存在 `line.startswith(...)`、`if "某段文字" in line` 这样的判断。

因此，当你准备大改一句带有演出意义的原文时：

1. 先全项目搜索这句原文。
2. 如果它也出现在 `tools/chapter1/build.py` 或 `tools/chapters/build.py` 的判断条件中，修改源稿后也要同步修改判断条件。
3. 重新生成后检查对应图片、CG、分支或玩法入口是否仍然出现。

普通对白润色大多不会触及这个问题，但场景标题、Gameplay 标记、分歧标记和关键演出句需要格外小心。

---

## 四、建议的安全修改流程

每次只改一个小范围，按下面顺序做：

1. 用 `Ctrl+Shift+F` 找到原句或节点 ID。
2. 确认正在修改真源，而不是开头带 `Generated by` 的生成结果。
3. 放入素材，并在素材清单登记资源 ID。
4. 修改正文或生成规则。
5. 需要时重新生成章节或地图文件。
6. 运行测试：

```powershell
npm test
```

7. 启动游戏实际检查：

```powershell
npm start
```

8. 从修改段落前一个节点开始试玩，至少检查：当前句、下一句、分支返回、图片切换和音乐停止点。

修改前最好先做一次 Git 提交。这样如果节点连接或生成结果出错，可以明确看到本次到底改了哪些行。

---

## 五、最短操作速查

### 新增背景图

1. 图片放进 `game/assets/images/backgrounds/`。
2. 在 `game/data/assets.js` 的 `images` 登记 ID 和路径。
3. 在范围内的每个对白节点写 `background: "该ID"`。
4. 范围后的第一个节点写下一张背景。

### 新增人物立绘

1. 透明图片放进 `game/assets/images/characters/`。
2. 在图片清单登记 ID。
3. 单人居中用 `portrait`；站位、缩放或多人用 `characters`。
4. 要让人物跨多句出现，每一句对应节点都要写人物字段；下一节点不写人物字段即消失。

### 新增 CG

1. 图片放进 `game/assets/images/cg/`。
2. 在图片清单登记 ID。
3. 在需要显示的每个节点写 `cg: "该ID"`。
4. 常配 `backgroundFit: "contain"`；范围结束后改回 `background`。

### 新增背景音乐

1. MP3 放进 `game/assets/audio/bgm/`。
2. 在 `game/data/assets.js` 的 `bgm` 登记 ID。
3. 起始对白节点写 `bgm: "该ID"`。
4. 中间节点不写，音乐会继续。
5. 结束处的对白节点写 `bgm: null`，或写另一个 ID 切歌。
6. 游戏中点击菜单里的“开启音乐”进行试听。

### 修一句剧本文案

1. 全项目搜索原句。
2. 序章直接改 `game/data/story/prologue.js` 或 `phone.js`。
3. 第一章改 `tools/chapter1/source.txt` 后运行第一章生成脚本。
4. 第二、三、最终章改对应的 `tools/chapters/*.txt` 后运行总生成脚本。
5. 不改节点 ID、`next`、分支 flag。
6. 运行 `npm test`，再实际试玩修改点。
