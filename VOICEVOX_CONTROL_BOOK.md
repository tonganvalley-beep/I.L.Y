# I.L.Y. VOICEVOX 配音编辑器项目总控书

更新：2026-09-17。总控：Astra。实现方案与代码：GPT-5.6 Sol / Terra。

## 1. 决策与当前状态

**需要独立总控书。** 这是长期配音制作工具，涉及批量文本、人工调音成果、持久化任务及游戏资产映射，不能用一次性文本转语音脚本替代。

T1、P1-P6 软件工作包已完成，2026-09-17 全项目技术验收通过，见 `docs/voicevox/reports/FINAL_ACCEPTANCE.md`。P1-P5 编辑器工作包已完成。2026-09-17 用户调度并完成 P6 游戏播放接入，见 `docs/voicevox/reports/P6.md`。实现及验证证据见各阶段报告。P5 已完成 5,000 条工作表、工程保存重开、批量故障恢复、导入与路径边界、本地 API、端口回退、真实引擎短句抽样和中文交付文档；详见 `docs/voicevox/reports/P5.md`。真实日语听感与音声库许可仍须在生产制作中人工确认，不把接口成功、WAV 结构校验或自动化测试标成听感验收完成。

关联文档：
- [游戏总控书](GAME_CONTROL_BOOK.md)：游戏现有约束。
- [Sol 技术勘查与实现建议](docs/voicevox/IMPLEMENTATION_PROPOSAL.md)：具体实现思路由 Sol 提出。
- [实现任务提示词](docs/voicevox/IMPLEMENTATION_PROMPTS.md)：后续模型逐阶段执行入口。

Astra 决定范围、优先级、验收和下一阶段提示词；Sol/Terra 提交实现方案、代码与证据。实现者可以提出替代方案，不能悄悄扩大范围或把未验证的引擎能力写成保证。

## 2. 已查证事实与限制

### 本机引擎

2026-09-16 通过本机 HTTP 接口读取：
- `http://127.0.0.1:50021/version` 返回 `0.25.2`。
- `/openapi.json` 返回 VOICEVOX Engine 接口描述。
- `/engine_manifest` 的 UUID 为 `074fc39e-678b-4c13-8916-ffca8d505d1d`。
- `/speakers` 返回 43 个说话人、127 个 `talk` 风格；这是当前安装快照，程序必须动态读取。
- 能力标记包括语速、全局音高、抑扬、音量、停顿、逐 mora 音高、音素时长、疑问句上扬、音色混合。
- `/audio_query`、`/accent_phrases`、`/mora_data`、`/mora_pitch`、`/mora_length`、`/synthesis` 等接口存在。
- `AudioQuery` 包含 `speedScale`、`pitchScale`、`intonationScale`、`volumeScale`、`prePhonemeLength`、`postPhonemeLength`、`pauseLength`、`pauseLengthScale`；句子中还有重音位置和 mora 数据。
- `kana` 在 AudioQuery 中是只读参考，合成时忽略；不能靠改这个字段实现读音编辑。

上述为初始勘查快照；P5 与最终验收已完成真实短句合成和 WAV 校验，人工听感验收及全量合成性能基准尚未完成。安装程序路径尚未确认，首版优先连接用户已启动的引擎，不依赖定位或改造原版 VOICEVOX。

### 游戏工程

- Node.js `v22.20.0`；现有游戏是普通 HTML/JS，本地服务为 `tools/serve.mjs`。
- 六篇基础剧情共 1,874 节点，其中 1,868 节点含文本或标题；这不是最终应配音句数，含旁白、演出和玩法说明。
- 游戏组装函数 `prepareChapter1()` 另加入 `ch2_photo` 摄影节点，因此组装后的基础图为 1,875 节点；导入应采用组装并应用已发布编辑后的有效视图，不能把任一总数硬编码。
- `game/data/story/script-edits.js` 有 279 条已发布记录。有效剧本必须复用现有覆盖、插入、删除和分类规则。
- `game/src/script-review-model.js` 存在 `reviewId` 与插入代理节点。实现时须验证稳定身份，不能仅按显示序号命名音频。
- `game/data/assets.js` 登记了 `assets/audio/voices/`，但注释明确语音尚未接线；导出音频不等于游戏已可播放。
- 本轮只读取项目和引擎信息，没有更改游戏数据、VOICEVOX 设置或用户词典。

### 配音语言与“情绪”边界

用户已确认：**日语配音，游戏保留中文字幕**。设计必须分离 `displayText`（原文/中文字幕）和 `speechText`（日语合成文本），并记录翻译校对状态；中文字幕变化时提醒复核译文和音频。日语翻译与校对是独立制作工作，不默认自动翻译全篇。

用户进一步明确制作顺序：**先生成各角色台词的日语版本文件，再交给 VOICEVOX 配音**。标准 VOICEVOX 按日语合成引擎使用；接受中文字符不代表能够正确朗读中文，也不代表具备翻译功能。

生产流程为：游戏有效中文剧本 → 按角色导出中日对照待译稿 → 翻译并校对日语 → 导入已校对的角色日语文件 → 试听、调音、批量合成 → 依据台词 ID 回接游戏。

角色日语文件应保留稳定台词 ID、章节/场景、原始顺序；翻译工作稿同时保留中文与必要的相邻对话上下文，避免脱离语境。角色拆分是交付形式，翻译需参考跨角色完整场景。优先使用结构化 CSV/TSV，纯 TXT 作为便捷导入；只有“每行一句日文”且无 ID 的文件不能可靠自动映射游戏，需要配套清单或人工确认。保留原中文台词，绝不以日文覆盖游戏中文字幕。未翻译/未校对的生产条目默认不进入正式批量合成，独立试听样例除外。

“情绪”第一层是引擎实际提供的角色风格，第二层是语速、抑扬、音高、停顿等组合预设。不存在可对任何音色通用的“悲伤 80%”接口。界面必须区分真实风格和人工调音预设；效果由试听决定。音色混合属于后续扩展，须验证目标配对，不能把能力标记当作任意组合均可用。

## 3. 产品目标与范围

首个完整可用版应让用户：导入各角色已准备好的日语文本 → 核对分句、角色及中文对应关系 → 选择声音/风格 → 批量设置基础参数 → 逐句试听与细调 → 保存项目 → 批量导出 WAV 和对应清单。中文剧本提取、翻译稿整理在进入合成之前完成，VOICEVOX 不负责翻译。

### 必须具备

1. 多文件 TXT 导入；CSV/TSV 结构化导入；导入预览、编码选择、错误行定位、顺序保留、重复处理。格式规则由 Sol 提案落定。
2. 台词列表支持角色、章节、状态筛选和多选；批量操作可撤销，覆盖人工调音前明确范围。
3. 游戏角色与引擎说话人/风格分别建模；从实际声音列表中映射，可保存角色默认值。
4. 单句及批量调整语速、音高、抑扬、音量、前后留白与停顿；数值可输入、可重置。
5. 单句试听；高级编辑重音位置、mora 音高、元音/辅音时长和短语停顿，尊重无声 mora 与可选辅音字段。
6. 独立工程文件保存/打开/备份，保留原文、配音文本、角色映射、参数、完整查询及人工编辑；刷新和重启不丢进度。
7. 批量生成队列、进度、暂停后续派发、失败重试、恢复和取消；缓存按合成输入判断失效。
8. 导出独立 WAV 与 manifest，以稳定 ID 关联台词；不以人类台词文本直接作为文件名。
9. 导入游戏有效剧本，排除删除项和默认不朗读的演出内容；修改后能识别音频过期。

### 界面方向

中文制作界面：左侧工程/章节/角色筛选，中间台词工作表，右侧声音与参数，底部试听及生成队列。原文与配音文本并列。高级调音按需展开，长列表只渲染可视范围或分页。具体组件、技术栈、数据契约由 Sol 提案。

### 后续扩展

词典管理、音色混合、原版 `.vvproj` 互通、桌面打包。不能在未研究原版编辑器工程版本格式前承诺 `.vvproj` 兼容；首版使用自己的有版本工程格式。

## 4. 工作量与分期

这是约 6 个实现阶段的中型工具项目，不是单页滑块演示。以下按可验收工作包估算，不承诺小时或日历工期。大批量日语翻译、全篇调音和人工听审另计。

| 阶段 | 责任 | 交付与通过条件 | 状态 |
| --- | --- | --- | --- |
| P0 勘查与立项 | Astra + Sol | 本书、具体技术提案、任务提示词；标明未知项 | 已完成 |
| T1 上游台词稿工具 | Sol | 提取有效剧本、按角色导出带 ID/上下文的中日对照待译稿、回导日语及校对状态 | 已完成；见 `docs/voicevox/reports/T1.md` |
| P1 最小端到端闭环 | Sol | 独立入口；引擎检测；TXT 导入预览；声音选择；基本参数；试听；工程保存/重开；单句 WAV | 已完成；见 `docs/voicevox/reports/P1.md` |
| P2 批量制作 | Sol 主导，Terra 可做有界模块 | CSV/TSV、多文件、筛选批量改值、角色预设、可恢复生成队列、导出清单 | 已完成；见 `docs/voicevox/reports/P2.md` |
| P3 精细调音 | 当前 Codex 会话 | 重音/逐 mora 音高与时长；撤销恢复；文本/风格改变的失效处理 | 已完成；见 `docs/voicevox/reports/P3.md` |
| P4 游戏剧本关联 | Sol | 已发布有效文本导入、稳定 ID、增量更新、过期检测及 manifest | 已完成；见 `docs/voicevox/reports/P4.md` |
| P5 加固与交付 | Sol / Terra | 5,000 条工作表验证、断连/重启恢复、操作指南、真实引擎抽样合成与人工听审记录 | 已完成；见 `docs/voicevox/reports/P5.md` |
| P6 游戏播放接入 | 当前 Codex 会话 | 场景切换/回滚/跳过的中断与不串音、音量、缺失语音降级 | 已完成；见 `docs/voicevox/reports/P6.md` |

编辑器顺序 P1 → P2 → P3 → P4 → P5；上游 T1 可先于或配合 P1 推进，实际生产配音前必须已有经校对的角色日语稿。T1 实现时复用 P4 的剧本身份设计，P4 再完成增量更新与资产关联，避免重复建设。翻译文本本身另按章节/场景分批制作，不属于引擎功能。P6 不阻塞编辑器交付。每阶段验收后 Astra 才下发下一阶段明确提示词；这是任务质量关卡，不要求用户重复授权正常开发。

## 5. 验收底线

- 保存与重开后，人工调音参数和句子身份保持一致；写盘失败不能显示“已保存”。
- 文本或引擎风格修改不能静默套用错位 mora。保留旧版本或可撤销快照，并明确重新分析会影响哪些编辑。
- 多文件同名、重复台词、重复源 ID、空行、中文标点、日文、UTF-8 BOM、Windows 换行及带换行 CSV 字段有明确规则和测试。
- 默认串行合成；更高并发依据实测决定。取消 HTTP 等待不宣称已停止引擎计算；旧请求不得覆盖新编辑。
- 队列失败逐句记录；恢复不能把上次中断的任务误判为成功。临时 WAV 完成校验后才登记可用。
- 游戏导入须验证 279 条已发布覆盖中的增删改语义；只按当前源哈希判断音频是否仍可用。
- 5,000 条用于导入、列表与队列调度验证，默认用模拟引擎测试大量故障；真实引擎只做小样，不默认全量消耗机器资源。
- 编辑器本机服务只绑定回环地址；写入限制在工程/导出目录，防止导入文件名造成目录穿越；不改变 VOICEVOX 全局设置。
- 对引擎修改的测试使用模拟环境；日语风格的听感只能记录实际试听结果，不能用 HTTP 200 代替。
- UI 流程实测；修改现有代码时运行相应回归，必要时 `npm test`。测试证据与未测项写入阶段报告。

## 6. 总控维护规则

每阶段完成后追加：执行模型、变更文件、启动命令、测试结果、已知问题、下一步。实现者提交阶段报告；Astra 审核后更新本书的状态与决策。计划路径及命令在实现前都必须标注“拟定”，不能让用户误以为已经可运行。

待确认项：角色与音色的审美选择；是否需要朗读旁白；首批真实导入样本的格式。前两阶段可以先用小份日语样例和独立显示文本推进，不替用户改写正式剧情。配音语言已确定为日语，中文字幕保留。

**下一步：** T1 与 P1-P6 已通过技术验收，可按 `docs/voicevox/QUICK_START.md` 进入实际日语翻译、选角、许可核对、逐句试听与生产制作。P6 已完成，发布经过校对的音频后刷新游戏即可播放；当前空 manifest 不代表全篇配音制作完成。默认语音开启、80% 音量；菜单/失焦中断后不续播，开启开关仅影响后续节点。手动推进和既有自动演出节奏不等待音频。
